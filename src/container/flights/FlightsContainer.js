import React from 'react';
import {FlightScreen} from '../../screens';

const FlightsContainer = ({navigation}) => {
  return <FlightScreen navigation={navigation} />;
};

export default FlightsContainer;
